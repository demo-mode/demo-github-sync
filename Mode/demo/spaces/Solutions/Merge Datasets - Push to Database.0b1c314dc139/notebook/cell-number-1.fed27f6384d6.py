datasets

# The script will error because we have removed all credentials, please go through and add your own and then run the script


pd.options.display.max_rows = 20

# Install Oauth to allow the script to authenticate within your database
from google.oauth2 import service_account
